{
  "name": "g360",
  "version": "1.0.0",
  "description": "google 360 test",
  "main": "scripts.js",
  "scripts": {
    "buildSass": "node-sass ./src/sass/main.scss ./src/main.css -w",
    "startServer": "live-server ./src",
    "start": "npm-run-all -p startServer buildSass",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.0",
    "npm-run-all": "^4.1.5"
  }
}
